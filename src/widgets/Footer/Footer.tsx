import React, { useState } from 'react';
import './Footer.scss';

const Navbar = () => {

    return (
        <div className="counter-container2">
         footer
        </div>
    );
};

export default Navbar;
